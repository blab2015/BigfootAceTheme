{% if sorted %}
    <i class="icon-chevron-{% if direction == 'asc' %}up{% else %}down{% endif %}"></i>
{% endif %}
<a{% for attr, value in options %} {{ attr }}="{{ value }}"{% endfor %}>{{ title }}</a>